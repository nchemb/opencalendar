# Paste-ready /goal (continue BookKit v2 in a fresh session, pair with auto mode)

/goal BookKit v2 (repo ~/Documents/Projects/bookkit, branch v2) is launch-ready per docs/REQUIREMENTS.md and .planning/v2-checklist.md. Read both, plus .planning/LAUNCH.md, first. Done when ALL hold and are shown in output: (1) `npx vitest run` exits 0 with ≥257 tests; (2) `npx playwright test` passes on desktop and mobile projects twice in a row; (3) `npx tsc --noEmit` and `npx next lint` clean; (4) every unchecked box in .planning/v2-checklist.md is either ticked with a one-line note or listed as blocked-on-owner (DB choice, Stripe live key, DNS, prod push, Calendly cancel). Constraints: never push to any main branch, never deploy, never touch production Supabase/Stripe/Resend, never run migrations against a non-local DB; site swap branches (buildwithneej + alphaflow `bookkit-v2-swap`) stay unpushed. Stop after 3 continuations on the same open item and report exactly what remains.
